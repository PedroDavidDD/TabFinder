// Lógica en segundo plano (puede quedar vacío por ahora)
// Se mantiene por si necesitas añadir funcionalidades posteriores
chrome.runtime.onInstalled.addListener(() => {
  console.log('Extensión instalada');
});